// require('dotenv').config()
// const MovieFinder = require('./models/note')

// Note.findAll().then((notes) => console.log(notes))
// MovieFinder.create('this is **important**').then((note) => console.log(note))

// Note.destroy(5).then((res) => console.log(res))

// Note.update(4, ' # This is new', '#feff9c')

// Note.findById(5)
//   .then((res) => console.log(res))
//   .catch((err) => console.log(err.message))
